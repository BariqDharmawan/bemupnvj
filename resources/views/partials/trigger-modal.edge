@if(type == 'anchor')
<a class="{{ classnya || 'btn btn-primary' }}" href="javascript:void(0);" 
data-toggle="modal" data-target="#modal-{{ targetModal }}">
    @if(icon)
        <span class="mr-2">{{ text }}</span>
        <i class="fas {{ icon }}"></i>
    @else
        <span>{{ text }}</span>
    @end
</a>
@else
<button class="{{ classnya || 'btn btn-primary ' + addClass  }}" 
data-toggle="modal" data-target="#modal-{{ targetModal }}">
    @if(icon)
        <span class="mr-2">{{ text }}</span>
        <i class="fas {{ icon }}"></i>
    @else
        <span>{{ text }}</span>
    @end
</button>
@endif