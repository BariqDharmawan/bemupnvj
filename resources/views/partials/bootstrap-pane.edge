<div class="tab-pane fade {{ isActive ? 'show active' : '' }} {{ addClass || '' }}" 
id="pills-{{ dashCase(tabName) }}" role="tabpanel" 
aria-labelledby="pills-{{ dashCase(tabName) }}-tab"
{{ $props.serializeExcept(['tabName', 'id']) }}>
    {{{ await $slots.main() }}}
</div>