<div class="tab-pane fade {{ isActive ? 'show active' : '' }} {{ addClass || '' }}" 
id="tab-{{ dashCase(tabName) }}" role="tabpanel" 
aria-labelledby="tab-{{ dashCase(tabName) }}-tab"
{{ $props.serializeExcept(['tabName', 'id']) }}>
    {{{ await $slots.main() }}}
</div>