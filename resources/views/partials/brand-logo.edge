<a href="{{ route('landing_page') }}" class="flex items-center lg:mr-auto">
    <img src="{{ aboutUs.logo }}" alt="{{ aboutUs.cabinet_name }}" 
        height="60px" width="60px" class="rounded-full brand-logo">
    <span class="ml-2">{{ aboutUs.cabinet_name }}</span>
</a>