<!DOCTYPE html>
<html lang="en">
<head>
    @include('partials/meta')
    <link rel="stylesheet" href="/assets/css/app.css">
    @!section('style')
</head>
<body class="font-roboto">
    <nav class="bg-white h-20 mb-16">
        <div class="container flex items-center h-full">
            @include('partials/brand-logo')
            <ul class="flex items-center space-x-4">
                @!component('partials/menu', {
                    text: 'Beranda',
                    link: route('landing_page')
                })
                @!component('partials/menu', {
                    text: 'Tentang',
                    link: route('about_us.index')
                })
                @component('partials/menu', {
                    text: 'Info Mahasiswa',
                    hasDropdown: true
                })
                    <ul class="flex flex-col absolute hide-el default-transition">
                        @!component('partials/menu', {
                            text: 'Info Untuk Mahasiswa',
                            link: route('student_info.index')
                        })
                        @!component('partials/menu', {
                            text: 'Scholarship and career',
                            link: route('student_info.scholareer')
                        })
                    </ul>
                @end
                @component('partials/menu', {
                    text: 'Events',
                    hasDropdown: true
                })
                    <ul class="flex flex-col absolute hide-el">
                        @!component('partials/menu', {
                            text: 'Mendatang',
                            link: route('event.upcoming')
                        })
                        @!component('partials/menu', {
                            text: 'Lalu',
                            link: route('event.past')
                        })
                    </ul>
                @end
                @!component('partials/menu', {
                    text: 'Berita',
                    link: route('news_portal')
                })
                @!component('partials/menu', {
                    text: 'Kontak',
                    link: route('contact_us.index')
                })
            </ul>
        </div>
    </nav>
    <header>
        @!section('header')
    </header>
    <main>
        @!section('body')
    </main>
    <footer>
        <div class="container">
            <div class="grid lg:grid-cols-2">
                <div>
                    @include('partials/brand-logo')
                    <ul>
                        @each((contact, key) in contact)
                            <li class="flex">
                                <div class="mr-2">
                                    <img src="{{ contact.logo }}" height="16px"
                                    alt="{{ `${contact.info} ${process.env.APP_NAME}` }}">
                                </div>
                                <span>{{ contact.info }}</span>
                            </li>
                        @endeach
                    </ul>
                </div>
                <div></div>
            </div>
        </div>
    </footer>
    <script src="/assets/js/app.js"></script>
</body>
</html>